{
  "name": "sidebar-organizer",
  "version": "1.0.0",
  "description": "Organise your Slack sidebar based on channel name rules",
  "type": "module",
  "scripts": {
    "organize": "tsx organize.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
